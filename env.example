# Public settings
NEXT_PUBLIC_DEFAULT_TENANT="demo"
NEXT_PUBLIC_SITE_URL="http://localhost:3002"

# AI assistant configuration
AI_KEYS=
AI_API_KEY=
AI_PROVIDER=compatible
AI_BASE_URL=
AI_MODEL=gpt-4o-mini

# NextAuth (required when auth enabled)
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Database (Neon)
# Add pooling params if needed for stability: ?pgbouncer=true&connect_timeout=10
DATABASE_URL=

# Admin token for protected routes in production (used by live-edit Save/Publish)
ADMIN_TOKEN=""

# Demo admin setup access code (protects credentials + promotion endpoints)
DEMO_ADMIN_ACCESS_CODE=""

# Preview notes:
# - Vercel preview branches derive tenant from host; use /menu (no query needed).
# - For production, live QR should target https://tccmenus.com/t/<slug>


