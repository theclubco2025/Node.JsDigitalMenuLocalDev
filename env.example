# Public settings
NEXT_PUBLIC_DEFAULT_TENANT="demo"
NEXT_PUBLIC_SITE_URL="http://localhost:3002"

# AI assistant configuration
AI_KEYS=
AI_API_KEY=
AI_PROVIDER=compatible
AI_BASE_URL=
AI_MODEL=gpt-4o-mini

# NextAuth (required when auth enabled)
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Database (Neon)
# Add pooling params if needed for stability: ?pgbouncer=true&connect_timeout=10
DATABASE_URL=

# Admin token for protected routes in production (used by live-edit Save/Publish)
ADMIN_TOKEN=""

# Stripe (for leasing / monthly billing)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_BASIC_PRICE_ID=""
STRIPE_PREMIUM_PRICE_ID=""
STRIPE_ENTERPRISE_PRICE_ID=""

# Stripe (customer food ordering)
# Separate from STRIPE_WEBHOOK_SECRET to avoid mixing subscription events with food-order events.
STRIPE_ORDERS_WEBHOOK_SECRET=""
STRIPE_ORDERS_SECRET_KEY="" # Dedicated Stripe secret for food orders (can be sk_test_ for live POC)
STRIPE_TEST_KEY="" # Preview/test env convenience (sk_test_...)
STRIPE_ORDERS_WEBHOOK_SECRET_TEST="" # Preview/test webhook secret (whsec_...)

# Live POC toggle: allow ordering UI + checkout without DB edits for specific tenants (comma-separated slugs).
# Example: ORDERING_POC_TENANTS="independentbarandgrille"
ORDERING_POC_TENANTS=""

# Kitchen Display PIN (required for KDS access)
KITCHEN_PIN=""

# Stripe (fixed offer: one-time onboarding + monthly subscription)
# - STRIPE_ONBOARDING_PRICE_ID should be a one-time Price
# - STRIPE_MONTHLY_PRICE_ID should be a recurring monthly Price (defaults to STRIPE_BASIC_PRICE_ID if unset)
STRIPE_ONBOARDING_PRICE_ID=""
STRIPE_MONTHLY_PRICE_ID=""

# Demo admin setup access code (protects credentials + promotion endpoints)
DEMO_ADMIN_ACCESS_CODE=""
DEMO_ADMIN_EMAIL="demo-admin@demo.local"
# Preview notes:
# - Vercel preview branches derive tenant from host; use /menu (no query needed).
# - For production, live QR should target https://tccmenus.com/t/<slug>


